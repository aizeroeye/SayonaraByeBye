$(document).ready(function() {

  // Cuando selecciono una imagen del DOM, en el popup se muestra el reproductor

  $('.greengreen').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.dokurochan').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.chobits').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.clannad').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.kanon').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.tamers').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.savers').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.souleater').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.mirainikki').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.amagamiss').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.nhk').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.steinsgate').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.lain').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.cowboybebop').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.gintama').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.dragonball').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.oremonogatari').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.bokunohero').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.fullmetal').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.deathnote').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.shiki').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.yuyuhakusho').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.mob').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.ben10').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

  $('.batmanbeyond').click(function(){ // cuando hago click para que aparezca el popup también aparece el reproductor
    $('.mediPlayer').mediaPlayer();
  });

});
